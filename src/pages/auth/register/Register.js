import { RegisterForm } from "../../../components/forms/RegisterForm"

export const RegisterPage = (props) => {
    return (
        <div className="column">
            <h2 className="title">SIGN-UP IN PAGE</h2>
            <RegisterForm />
        </div>
    );
};